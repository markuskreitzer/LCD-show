#!/bin/bash
set -euo pipefail

cat >&2 <<'EOF'
The legacy MHS3528 installer is disabled because it makes unsafe system-wide changes.
Use these commands instead:
  ./MHS35-safe --dry-run
  sudo ./MHS35-safe --install
EOF
exit 1
